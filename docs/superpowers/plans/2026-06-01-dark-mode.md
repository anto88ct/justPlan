# Dark Mode / Theme Toggle — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a working dark/light theme toggle in Settings that persists via localStorage and applies Tailwind dark-mode classes throughout the main app shell.

**Architecture:** Enable Tailwind `darkMode: 'class'`, create `ThemeService` (signal + effect) that toggles the `dark` class on `<html>`, wire the existing settings toggle (key `'dark'`) to the service, then add `dark:` variants to every color token in `app-layout.component.ts`. Child components (wizard, dashboard, report, profile) are out of scope.

**Tech Stack:** Angular 17, Tailwind CSS v3, Angular signals, localStorage

---

## File Map

| File | Action |
|---|---|
| `tailwind.config.js` | Modify — add `darkMode: 'class'` |
| `src/styles.css` | Modify — add dark scrollbar via `.dark` selector |
| `src/app/services/theme.service.ts` | Create — ThemeService signal |
| `src/app/services/theme.service.spec.ts` | Create — unit tests |
| `src/app/components/app-layout/app-layout.component.ts` | Modify — inject service, wire toggle, add dark variants |

---

### Task 1: Enable Tailwind dark mode

**Files:**
- Modify: `tailwind.config.js`

- [ ] **Step 1: Add darkMode config**

Open `tailwind.config.js` and add `darkMode: 'class'` at the top level:

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',      // ← ADD THIS LINE
  content: [
    "./src/**/*.{html,ts}",
  ],
  theme: {
    extend: {
      // ... rest unchanged
```

- [ ] **Step 2: Commit**

```bash
git add tailwind.config.js
git commit -m "feat: enable Tailwind darkMode class strategy"
```

---

### Task 2: Create ThemeService

**Files:**
- Create: `src/app/services/theme.service.ts`
- Create: `src/app/services/theme.service.spec.ts`

- [ ] **Step 1: Write failing test**

Create `src/app/services/theme.service.spec.ts`:

```typescript
import { TestBed } from '@angular/core/testing';
import { ThemeService } from './theme.service';

describe('ThemeService', () => {
  let service: ThemeService;

  beforeEach(() => {
    localStorage.clear();
    document.documentElement.classList.remove('dark');
    TestBed.configureTestingModule({});
    service = TestBed.inject(ThemeService);
  });

  afterEach(() => {
    document.documentElement.classList.remove('dark');
    localStorage.clear();
  });

  it('defaults to light mode when localStorage is empty', () => {
    expect(service.dark()).toBeFalse();
    expect(document.documentElement.classList.contains('dark')).toBeFalse();
  });

  it('toggle() switches to dark mode', () => {
    service.toggle();
    expect(service.dark()).toBeTrue();
    expect(document.documentElement.classList.contains('dark')).toBeTrue();
    expect(localStorage.getItem('theme')).toBe('dark');
  });

  it('toggle() twice returns to light mode', () => {
    service.toggle();
    service.toggle();
    expect(service.dark()).toBeFalse();
    expect(document.documentElement.classList.contains('dark')).toBeFalse();
    expect(localStorage.getItem('theme')).toBe('light');
  });

  it('reads dark state from localStorage on init', () => {
    localStorage.setItem('theme', 'dark');
    TestBed.resetTestingModule();
    TestBed.configureTestingModule({});
    const freshService = TestBed.inject(ThemeService);
    expect(freshService.dark()).toBeTrue();
    expect(document.documentElement.classList.contains('dark')).toBeTrue();
  });
});
```

- [ ] **Step 2: Run test — expect failure**

```bash
npx ng test --include="**/theme.service.spec.ts" --watch=false
```
Expected: `Cannot find module './theme.service'`

- [ ] **Step 3: Implement ThemeService**

Create `src/app/services/theme.service.ts`:

```typescript
import { Injectable, signal, effect } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ThemeService {
  dark = signal<boolean>(
    typeof localStorage !== 'undefined'
      ? localStorage.getItem('theme') === 'dark'
      : false
  );

  constructor() {
    this.applyTheme(this.dark());
    effect(() => this.applyTheme(this.dark()));
  }

  toggle(): void {
    this.dark.update(v => !v);
  }

  private applyTheme(isDark: boolean): void {
    document.documentElement.classList.toggle('dark', isDark);
    if (typeof localStorage !== 'undefined') {
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
    }
  }
}
```

- [ ] **Step 4: Run test — expect pass**

```bash
npx ng test --include="**/theme.service.spec.ts" --watch=false
```
Expected: 4 specs, 0 failures

- [ ] **Step 5: Commit**

```bash
git add src/app/services/theme.service.ts src/app/services/theme.service.spec.ts
git commit -m "feat: add ThemeService with signal-based dark mode toggle"
```

---

### Task 3: Wire settings toggle in AppLayoutComponent

**Files:**
- Modify: `src/app/components/app-layout/app-layout.component.ts`

- [ ] **Step 1: Inject ThemeService and add `ngOnInit`**

Add import at top of file:
```typescript
import { Component, signal, computed, inject, HostBinding, OnInit } from '@angular/core';
import { ThemeService } from '../../services/theme.service';
```

In the class body, add:
```typescript
readonly themeService = inject(ThemeService);
```

Change class declaration to implement `OnInit`:
```typescript
export class AppLayoutComponent implements OnInit {
```

Add `ngOnInit` method (after existing fields):
```typescript
ngOnInit(): void {
  const darkItem = this.settingSections
    .flatMap(s => s.items)
    .find(i => i.key === 'dark');
  if (darkItem) darkItem.enabled = this.themeService.dark();
}
```

- [ ] **Step 2: Add toggleSettingItem method**

Add this method to the class (near other toggle methods):
```typescript
toggleSettingItem(item: SettingItem): void {
  if (item.key === 'dark') {
    this.themeService.toggle();
    item.enabled = this.themeService.dark();
  } else {
    item.enabled = !item.enabled;
  }
}
```

- [ ] **Step 3: Update settings template toggle**

Find the settings toggle button in the template (inside `@if (currentView() === 'impostazioni')`).

**Find:**
```html
(click)="item.enabled = !item.enabled"
```

**Replace with:**
```html
(click)="toggleSettingItem(item)"
```

- [ ] **Step 4: Commit**

```bash
git add src/app/components/app-layout/app-layout.component.ts
git commit -m "feat: wire settings dark toggle to ThemeService"
```

---

### Task 4: Add dark variants — main shell (topbar, AI panel, resize handle)

**Files:**
- Modify: `src/app/components/app-layout/app-layout.component.ts` (template)

All changes are inside the `template: \`` string. Find each element by its existing classes and add `dark:` variants.

- [ ] **Step 1: Main `<main>` element**

**Find:**
```html
<main class="flex-1 flex flex-col overflow-hidden bg-zinc-50 min-w-0">
```
**Replace with:**
```html
<main class="flex-1 flex flex-col overflow-hidden bg-zinc-50 dark:bg-zinc-950 min-w-0">
```

- [ ] **Step 2: Topbar `<header>`**

**Find:**
```html
<header class="h-12 bg-white border-b border-zinc-100 flex items-center px-3 md:px-5 gap-1 md:gap-2 flex-shrink-0 shadow-sm overflow-hidden">
```
**Replace with:**
```html
<header class="h-12 bg-white dark:bg-zinc-900 border-b border-zinc-100 dark:border-zinc-800 flex items-center px-3 md:px-5 gap-1 md:gap-2 flex-shrink-0 shadow-sm overflow-hidden">
```

- [ ] **Step 3: Hamburger button in topbar**

**Find:**
```html
class="lg:hidden flex items-center justify-center w-8 h-8 rounded-lg hover:bg-zinc-100 transition-colors flex-shrink-0">
```
**Replace with:**
```html
class="lg:hidden flex items-center justify-center w-8 h-8 rounded-lg hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-colors flex-shrink-0">
```

- [ ] **Step 4: Topbar breadcrumb text**

**Find:**
```html
<span class="text-zinc-400">Businext Plan</span>
```
**Replace with:**
```html
<span class="text-zinc-400 dark:text-zinc-500">Businext Plan</span>
```

**Find:**
```html
<span class="text-zinc-700 font-semibold">{{ currentTitle() }}</span>
```
**Replace with:**
```html
<span class="text-zinc-700 dark:text-zinc-300 font-semibold">{{ currentTitle() }}</span>
```

- [ ] **Step 5: Topbar buttons (Modifica, PDF)**

**Find:**
```html
class="flex items-center gap-1.5 text-xs text-zinc-500 hover:text-zinc-800 px-2 sm:px-3 py-1.5 rounded-lg hover:bg-zinc-100 transition-all font-body flex-shrink-0">
```
**Replace with:**
```html
class="flex items-center gap-1.5 text-xs text-zinc-500 dark:text-zinc-400 hover:text-zinc-800 dark:hover:text-zinc-200 px-2 sm:px-3 py-1.5 rounded-lg hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-all font-body flex-shrink-0">
```

**Find (PDF button):**
```html
class="flex items-center gap-1.5 text-xs text-zinc-600 px-2 sm:px-3 py-1.5 rounded-lg border border-zinc-200 hover:border-zinc-300 bg-white transition-all font-body flex-shrink-0">
```
**Replace with:**
```html
class="flex items-center gap-1.5 text-xs text-zinc-600 dark:text-zinc-400 px-2 sm:px-3 py-1.5 rounded-lg border border-zinc-200 dark:border-zinc-700 hover:border-zinc-300 dark:hover:border-zinc-600 bg-white dark:bg-zinc-800 transition-all font-body flex-shrink-0">
```

- [ ] **Step 6: AI panel — mobile header**

**Find:**
```html
<div class="flex items-center justify-between px-4 h-12 border-b border-zinc-100 flex-shrink-0 bg-white">
```
**Replace with:**
```html
<div class="flex items-center justify-between px-4 h-12 border-b border-zinc-100 dark:border-zinc-800 flex-shrink-0 bg-white dark:bg-zinc-900">
```

**Find (mobile close button):**
```html
class="w-8 h-8 flex items-center justify-center rounded-lg text-zinc-500 hover:bg-zinc-100 transition-colors">
```
**Replace with:**
```html
class="w-8 h-8 flex items-center justify-center rounded-lg text-zinc-500 dark:text-zinc-400 hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-colors">
```

- [ ] **Step 7: AI panel — desktop toolbar**

**Find:**
```html
<div class="hidden lg:flex items-center justify-between px-3 h-10 border-b border-zinc-100 flex-shrink-0 bg-white">
```
**Replace with:**
```html
<div class="hidden lg:flex items-center justify-between px-3 h-10 border-b border-zinc-100 dark:border-zinc-800 flex-shrink-0 bg-white dark:bg-zinc-900">
```

**Find (desktop toolbar icon buttons, there are 3 — the `w-7 h-7` buttons):**
```html
class="w-7 h-7 flex items-center justify-center rounded-lg text-zinc-400
                                   hover:text-zinc-700 hover:bg-zinc-100 transition-colors">
```
Replace all 3 occurrences with:
```html
class="w-7 h-7 flex items-center justify-center rounded-lg text-zinc-400 dark:text-zinc-500
                                   hover:text-zinc-700 dark:hover:text-zinc-300 hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-colors">
```

- [ ] **Step 8: AI panel — desktop resize handle**

**Find:**
```html
class="hidden lg:flex w-2 flex-shrink-0 items-center justify-center
                          cursor-col-resize group border-l border-zinc-100 hover:border-brand-300
                          transition-colors select-none"
```
**Replace with:**
```html
class="hidden lg:flex w-2 flex-shrink-0 items-center justify-center
                          cursor-col-resize group border-l border-zinc-100 dark:border-zinc-800 hover:border-brand-300
                          transition-colors select-none"
```

**Find (the drag handle pill):**
```html
<div class="w-0.5 h-8 rounded-full bg-zinc-200 group-hover:bg-brand-400 transition-colors"></div>
```
**Replace with:**
```html
<div class="w-0.5 h-8 rounded-full bg-zinc-200 dark:bg-zinc-700 group-hover:bg-brand-400 transition-colors"></div>
```

- [ ] **Step 9: Commit**

```bash
git add src/app/components/app-layout/app-layout.component.ts
git commit -m "feat: add dark mode variants to main shell and topbar"
```

---

### Task 5: Add dark variants — Panoramica view

**Files:**
- Modify: `src/app/components/app-layout/app-layout.component.ts` (template, inside `@if (currentView() === 'panoramica')`)

- [ ] **Step 1: Live KPI cards**

**Find:**
```html
<div class="animate-kpi-in bg-white rounded-2xl border border-zinc-100 shadow-card
                                     hover:shadow-card-hover hover:-translate-y-0.5 transition-all duration-200 p-4"
```
**Replace with:**
```html
<div class="animate-kpi-in bg-white dark:bg-zinc-900 rounded-2xl border border-zinc-100 dark:border-zinc-800 shadow-card
                                     hover:shadow-card-hover hover:-translate-y-0.5 transition-all duration-200 p-4"
```

**Find (KPI label text):**
```html
<p class="text-[11px] text-zinc-400 font-medium font-body truncate mb-0.5">{{ kpi.label }}</p>
<p class="text-lg font-bold font-mono text-zinc-900 number-highlight">{{ kpi.value }}</p>
```
**Replace with:**
```html
<p class="text-[11px] text-zinc-400 dark:text-zinc-500 font-medium font-body truncate mb-0.5">{{ kpi.label }}</p>
<p class="text-lg font-bold font-mono text-zinc-900 dark:text-zinc-100 number-highlight">{{ kpi.value }}</p>
```

- [ ] **Step 2: Tutorial section heading**

**Find:**
```html
<h2 class="text-base font-bold text-zinc-900 font-display">Guida rapida</h2>
```
**Replace with:**
```html
<h2 class="text-base font-bold text-zinc-900 dark:text-zinc-100 font-display">Guida rapida</h2>
```

**Find:**
```html
<span class="text-[10px] text-zinc-400 font-body hidden sm:block">
```
**Replace with:**
```html
<span class="text-[10px] text-zinc-400 dark:text-zinc-500 font-body hidden sm:block">
```

- [ ] **Step 3: Step cards**

**Find:**
```html
class="step-card animate-kpi-in bg-white rounded-2xl border shadow-card
                               hover:shadow-card-hover hover:-translate-y-0.5 transition-all duration-200
                               p-4 text-left cursor-pointer group relative overflow-hidden"
```
**Replace with:**
```html
class="step-card animate-kpi-in bg-white dark:bg-zinc-900 rounded-2xl border shadow-card
                               hover:shadow-card-hover hover:-translate-y-0.5 transition-all duration-200
                               p-4 text-left cursor-pointer group relative overflow-hidden"
```

The `[ngClass]` for step done border stays as is.

**Find (step large number bg decoration):**
```html
[ngClass]="step.done ? 'text-emerald-50' : 'text-zinc-50'">
```
**Replace with:**
```html
[ngClass]="step.done ? 'text-emerald-50 dark:text-emerald-900/20' : 'text-zinc-50 dark:text-zinc-800'">
```

**Find (step title):**
```html
<p class="text-[12px] font-bold text-zinc-900 font-display mb-1 leading-snug relative z-10">
```
**Replace with:**
```html
<p class="text-[12px] font-bold text-zinc-900 dark:text-zinc-100 font-display mb-1 leading-snug relative z-10">
```

**Find (step desc):**
```html
<p class="text-[10px] text-zinc-400 font-body leading-relaxed relative z-10 line-clamp-2">
```
**Replace with:**
```html
<p class="text-[10px] text-zinc-400 dark:text-zinc-500 font-body leading-relaxed relative z-10 line-clamp-2">
```

- [ ] **Step 4: AI tip banner**

**Find:**
```html
<div class="mt-3 flex items-center gap-3 px-4 py-3 bg-brand-50 rounded-2xl border border-brand-100 animate-fade-in"
```
**Replace with:**
```html
<div class="mt-3 flex items-center gap-3 px-4 py-3 bg-brand-50 dark:bg-brand-950/40 rounded-2xl border border-brand-100 dark:border-brand-900/50 animate-fade-in"
```

**Find (AI tip title text):**
```html
<p class="text-xs font-semibold text-brand-700 font-body">Suggerimento</p>
<p class="text-[11px] text-brand-600 font-body leading-relaxed">
```
**Replace with:**
```html
<p class="text-xs font-semibold text-brand-700 dark:text-brand-300 font-body">Suggerimento</p>
<p class="text-[11px] text-brand-600 dark:text-brand-400 font-body leading-relaxed">
```

**Find (Apri AI button inside banner):**
```html
class="flex-shrink-0 flex items-center gap-1.5 text-[11px] font-semibold text-brand-600
                                   hover:text-brand-800 px-3 py-1.5 bg-white rounded-xl border border-brand-200
                                   hover:border-brand-300 transition-all font-body whitespace-nowrap">
```
**Replace with:**
```html
class="flex-shrink-0 flex items-center gap-1.5 text-[11px] font-semibold text-brand-600 dark:text-brand-400
                                   hover:text-brand-800 dark:hover:text-brand-200 px-3 py-1.5 bg-white dark:bg-zinc-900 rounded-xl border border-brand-200 dark:border-brand-800
                                   hover:border-brand-300 transition-all font-body whitespace-nowrap">
```

- [ ] **Step 5: Commit**

```bash
git add src/app/components/app-layout/app-layout.component.ts
git commit -m "feat: add dark mode variants to Panoramica view"
```

---

### Task 6: Add dark variants — Scenari and Piani Salvati views

**Files:**
- Modify: `src/app/components/app-layout/app-layout.component.ts`

- [ ] **Step 1: Scenari section headings**

Inside `@if (currentView() === 'scenari')`, find:

**Find:**
```html
<h1 class="text-2xl font-bold text-zinc-900 font-display mb-1">What-If Simulator</h1>
<p class="text-sm text-zinc-500 font-body mb-7">
```
**Replace with:**
```html
<h1 class="text-2xl font-bold text-zinc-900 dark:text-zinc-100 font-display mb-1">What-If Simulator</h1>
<p class="text-sm text-zinc-500 dark:text-zinc-400 font-body mb-7">
```

- [ ] **Step 2: Scenari cards**

**Find:**
```html
class="w-full bg-white rounded-2xl border border-zinc-100 p-4 shadow-card text-left
                             hover:border-brand-200 hover:shadow-card-hover hover:-translate-y-0.5
                             transition-all duration-200 group"
```
**Replace with:**
```html
class="w-full bg-white dark:bg-zinc-900 rounded-2xl border border-zinc-100 dark:border-zinc-800 p-4 shadow-card text-left
                             hover:border-brand-200 dark:hover:border-brand-800 hover:shadow-card-hover hover:-translate-y-0.5
                             transition-all duration-200 group"
```

**Find (scenario title):**
```html
<p class="text-sm font-semibold text-zinc-800 font-body group-hover:text-brand-700 transition-colors">
```
**Replace with:**
```html
<p class="text-sm font-semibold text-zinc-800 dark:text-zinc-200 font-body group-hover:text-brand-700 dark:group-hover:text-brand-400 transition-colors">
```

**Find (scenario subtitle):**
```html
<p class="text-xs text-zinc-400 font-body mt-0.5 italic truncate">
```
**Replace with:**
```html
<p class="text-xs text-zinc-400 dark:text-zinc-500 font-body mt-0.5 italic truncate">
```

- [ ] **Step 3: Scenari no-plan warning banner**

**Find:**
```html
<div class="mt-6 p-4 bg-amber-50 rounded-2xl border border-amber-100"
```
**Replace with:**
```html
<div class="mt-6 p-4 bg-amber-50 dark:bg-amber-950/30 rounded-2xl border border-amber-100 dark:border-amber-900/40"
```

**Find:**
```html
<p class="text-sm text-amber-700 font-body">
```
**Replace with:**
```html
<p class="text-sm text-amber-700 dark:text-amber-400 font-body">
```

- [ ] **Step 4: Piani Salvati headings**

Inside `@if (currentView() === 'piani-salvati')`, find:

**Find:**
```html
<h1 class="text-2xl font-bold text-zinc-900 font-display">I Miei Piani</h1>
<p class="text-sm text-zinc-500 font-body mt-1">
```
**Replace with:**
```html
<h1 class="text-2xl font-bold text-zinc-900 dark:text-zinc-100 font-display">I Miei Piani</h1>
<p class="text-sm text-zinc-500 dark:text-zinc-400 font-body mt-1">
```

- [ ] **Step 5: Piani Salvati — empty state**

**Find:**
```html
<div class="w-16 h-16 rounded-2xl bg-zinc-100 flex items-center justify-center mx-auto mb-4 float-anim">
```
**Replace with:**
```html
<div class="w-16 h-16 rounded-2xl bg-zinc-100 dark:bg-zinc-800 flex items-center justify-center mx-auto mb-4 float-anim">
```

**Find (empty state texts):**
```html
<p class="text-sm font-semibold text-zinc-600 font-display mb-1">Nessun piano salvato</p>
<p class="text-xs text-zinc-400 font-body">Genera un piano e premi "Salva Business Plan".</p>
```
**Replace with:**
```html
<p class="text-sm font-semibold text-zinc-600 dark:text-zinc-400 font-display mb-1">Nessun piano salvato</p>
<p class="text-xs text-zinc-400 dark:text-zinc-500 font-body">Genera un piano e premi "Salva Business Plan".</p>
```

- [ ] **Step 6: Piani Salvati — plan cards**

**Find:**
```html
class="bg-white rounded-2xl border border-zinc-100 shadow-card hover:shadow-card-hover
                                  hover:-translate-y-0.5 transition-all duration-200 overflow-hidden"
```
**Replace with:**
```html
class="bg-white dark:bg-zinc-900 rounded-2xl border border-zinc-100 dark:border-zinc-800 shadow-card hover:shadow-card-hover
                                  hover:-translate-y-0.5 transition-all duration-200 overflow-hidden"
```

**Find (card header border):**
```html
<div class="px-5 pt-5 pb-4 border-b border-zinc-50">
```
**Replace with:**
```html
<div class="px-5 pt-5 pb-4 border-b border-zinc-50 dark:border-zinc-800">
```

**Find (plan name text):**
```html
<p class="text-sm font-bold text-zinc-900 font-display truncate">{{ plan.name }}</p>
<p class="text-xs text-zinc-400 font-body mt-0.5">{{ formatDate(plan.savedAt) }}</p>
```
**Replace with:**
```html
<p class="text-sm font-bold text-zinc-900 dark:text-zinc-100 font-display truncate">{{ plan.name }}</p>
<p class="text-xs text-zinc-400 dark:text-zinc-500 font-body mt-0.5">{{ formatDate(plan.savedAt) }}</p>
```

**Find (KPI label texts inside plan card, there are 4):**
```html
<p class="text-xs text-zinc-400 font-body">Fatturato Y1</p>
```
Add `dark:text-zinc-500` to all 4 instances inside the plan card KPI grid:
```html
<p class="text-xs text-zinc-400 dark:text-zinc-500 font-body">Fatturato Y1</p>
```
Do the same for `Utile Netto`, `EBITDA Y1`, `Cash Runway`.

**Find (plan value text — neutral colored ones):**
```html
<p class="text-sm font-bold text-zinc-800 font-mono mt-0.5">{{ formatK(plan.kpi.fatturatoTotale) }}</p>
```
**Replace with:**
```html
<p class="text-sm font-bold text-zinc-800 dark:text-zinc-200 font-mono mt-0.5">{{ formatK(plan.kpi.fatturatoTotale) }}</p>
```
Do the same for `utileNetto` neutral case and `cashRunway`.

**Find (Carica Piano button):**
```html
class="w-full flex items-center justify-center gap-2 px-4 py-2.5
                                         bg-zinc-950 hover:bg-zinc-800 text-white text-xs font-semibold
                                         rounded-xl transition-all duration-200 font-body">
```
**Replace with:**
```html
class="w-full flex items-center justify-center gap-2 px-4 py-2.5
                                         bg-zinc-950 dark:bg-zinc-800 hover:bg-zinc-800 dark:hover:bg-zinc-700 text-white text-xs font-semibold
                                         rounded-xl transition-all duration-200 font-body">
```

- [ ] **Step 7: Commit**

```bash
git add src/app/components/app-layout/app-layout.component.ts
git commit -m "feat: add dark mode variants to Scenari and Piani Salvati views"
```

---

### Task 7: Add dark variants — Impostazioni view

**Files:**
- Modify: `src/app/components/app-layout/app-layout.component.ts`

- [ ] **Step 1: Impostazioni headings**

Inside `@if (currentView() === 'impostazioni')`, find:

**Find:**
```html
<h1 class="text-2xl font-bold text-zinc-900 font-display mb-7">Impostazioni</h1>
```
**Replace with:**
```html
<h1 class="text-2xl font-bold text-zinc-900 dark:text-zinc-100 font-display mb-7">Impostazioni</h1>
```

- [ ] **Step 2: Settings section cards**

**Find:**
```html
<div class="bg-white rounded-2xl border border-zinc-100 p-5 shadow-card"
```
**Replace with:**
```html
<div class="bg-white dark:bg-zinc-900 rounded-2xl border border-zinc-100 dark:border-zinc-800 p-5 shadow-card"
```

- [ ] **Step 3: Setting item texts**

**Find:**
```html
<p class="text-sm font-medium text-zinc-700 font-body">{{ item.label }}</p>
<p class="text-xs text-zinc-400 font-body mt-0.5">{{ item.desc }}</p>
```
**Replace with:**
```html
<p class="text-sm font-medium text-zinc-700 dark:text-zinc-300 font-body">{{ item.label }}</p>
<p class="text-xs text-zinc-400 dark:text-zinc-500 font-body mt-0.5">{{ item.desc }}</p>
```

- [ ] **Step 4: Toggle button off-state background**

The toggle button background switches between `bg-brand-600` (on) and `bg-zinc-200` (off). The `[ngClass]` already handles this dynamically.

**Find the [ngClass] array for the toggle button:**
```html
item.enabled ? 'bg-brand-600' : 'bg-zinc-200'
```
**Replace with:**
```html
item.enabled ? 'bg-brand-600' : 'bg-zinc-200 dark:bg-zinc-700'
```

- [ ] **Step 5: Commit**

```bash
git add src/app/components/app-layout/app-layout.component.ts
git commit -m "feat: add dark mode variants to Impostazioni view"
```

---

### Task 8: Dark scrollbar in styles.css

**Files:**
- Modify: `src/styles.css`

- [ ] **Step 1: Add dark mode scrollbar**

Open `src/styles.css`. After the existing `.scrollbar-thin::-webkit-scrollbar-thumb` block, add:

```css
/* Dark mode scrollbar */
.dark .scrollbar-thin {
  scrollbar-color: #3f3f46 transparent;
}
.dark .scrollbar-thin::-webkit-scrollbar-thumb {
  background-color: #3f3f46;
}
```

- [ ] **Step 2: Commit**

```bash
git add src/styles.css
git commit -m "feat: add dark mode scrollbar styles"
```

---

### Task 9: Smoke test

- [ ] **Step 1: Run app**

```bash
npx ng serve
```
Navigate to `http://localhost:4200`.

- [ ] **Step 2: Verify light mode (default)**
  - App loads in light mode (white backgrounds, dark text)
  - No `dark` class on `<html>` in DevTools Elements panel
  - localStorage has no `theme` key or `theme=light`

- [ ] **Step 3: Toggle dark mode in Settings**
  - Navigate to Impostazioni
  - Toggle "Tema Scuro" switch ON
  - `dark` class appears on `<html>`
  - Main area: `bg-zinc-950` (very dark bg)
  - Topbar: dark bg, light text
  - Cards in Panoramica: dark bg, light text
  - Scrollbar: darker color

- [ ] **Step 4: Verify persistence**
  - Reload page
  - Dark mode persists (localStorage `theme=dark`)
  - Toggle OFF → light mode returns, localStorage `theme=light`

- [ ] **Step 5: Responsive check**
  - Open DevTools mobile emulation (375px width)
  - Toggle works on mobile
  - Hamburger menu shows dark sidebar on mobile overlay
  - No horizontal overflow

- [ ] **Step 6: Final commit if any fixes needed**

```bash
git add -p
git commit -m "fix: dark mode visual adjustments from smoke test"
```
